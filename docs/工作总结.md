## 2026-07-01 航道路费与支付计划模块收口
- 以 `Assets/YC/Domain/Travel/RouteTollService.cs` 为准：探索与采集共用同一套路费计划构建、接收者校验、同区域 `paymentKey` 去重、公路免费和实际扣款逻辑；`ExplorationTravelPayment` 继续作为现有结果记录，避免应用层和表现层连锁改名。
- 以 `Assets/YC/Domain/Exploration/ExplorationService.cs`、`Assets/YC/Domain/Harvest/ResourceCollectionService.cs` 为准：探索服务只保留探索目标、事件、资源指示物和影响力放置流程；采集服务只保留采集目标、路线连通、本阶段金券快照和资源发放流程，航道路费细节交给 `RouteTollService`。
- 路费模块支持 `RouteId` 与 `SharedRegion` 两种 paymentKey 模式；探索和采集当前使用 `SharedRegion`，保留“己方航道影响力免费”“对手航道可指定接收者”“无对手支付给供应区”“被公路覆盖免费”的行为。
- 已补充 EditMode 覆盖：`TravelCostServiceTests` 验证 routeId 与同区域 paymentKey 模式、己方航道覆盖免费；`ResourceCollectionServiceTests` 验证采集对手路费、同区域重复航道只付一次、非法接收者拒绝；既有 `ExploreLocationCommandHandlerTests` 继续覆盖探索路费和非法接收者。
- `LocalhostAutoplayRunner` 的自动跑局供应区固定注入一张低价且有设施分的设施牌，避免同区域路费去重后不再依赖重复路费收入才能覆盖建设与最终设施计分。

## 2026-07-01 建设牌与城市样式牌素材拆分
- 以 `docs/建设牌输出` 为准：已从 `游城拓荒/素材/建设牌堆.jpg` 拆出 41 张建筑卡片、1 张代表重复 4 次的 `核心指挥塔`、3 张不同颜色的 `延伸枢纽`，并生成 `building_cards_manifest.json` 与 `building_cards_manifest.md`；3 张 `企业办事处` 已按要求暂不记录。
- 以 `docs/城市样式牌输出` 为准：已按 `城市样式牌1待拆.jpg` 与 `城市样式牌2待拆.jpg` 的红圈范围拆出 6 张城市样式牌，记录分数、名字、描述、条件、特殊行动效果和宣告限制，并生成 `city_style_cards_manifest.json` 与 `city_style_cards_manifest.md`。
- 以 `tools/extract_building_cards.py` 与 `tools/extract_city_style_cards.py` 为准：本次素材裁剪和清单生成已有可复现脚本；脚本只写出目标文件，不执行删除操作。
- 当前边界：纯图标效果仍需结合规则书正式术语复核；这些牌面数据尚未接入运行时 `FacilityCardDatabase`、`CityStyleDatabase` 或特殊行动系统。

## 2026-06-28 长挂机字体自检入口
- 以 `Assets/YC/Presentation/FontUtility.cs`、`Assets/YC/Presentation/FontHealthCheckRunner.cs`、`Assets/YC/Editor/FontHealthCheckMenu.cs` 为准：开发/测试环境现已支持主动触发字体健康检查、输出托管字体与纹理快照，并可模拟一次托管字体重建路径。
- 以 `Assets/YC/Presentation/StartMenuController.cs` 为准：命令行新增 `--yc-dev-font-health-check` 与 `--yc-dev-font-health-check-simulate`，可在 Editor 或 Development Build 启动时直接跑字体自检。
- 以 `Assets/YC/Tests/EditMode/FontUtilityTests.cs` 与 `tools/Run-FontHealthCheckValidation.ps1` 为准：字体单例缓存回归之外，已补“快照生成”和“模拟重建”两项最小 EditMode 验证；最近一次结果为 `4 passed / 0 failed`，日志为 `Logs/EditModeTests/editmode-20260627_140309.xml`。
# 当前工程进展

## 2026-06-30 事件选择表现层切换到 PendingCardSession 优先
- 以 `Assets/YC/Domain/Cards/CardFlowStateAdapter.cs`、`Assets/YC/Presentation/MobileCityInteractionController.cs`、`Assets/YC/Presentation/EventChoiceDialog.cs` 为准：事件选择表现层现在通过待选快照优先消费 `PendingCardSessionState`，旧 `PendingChoiceState` 只作为兼容 fallback，不再作为 UI 主事实源。
- 以 `Assets/YC/Application/Setup/SetupCommandHandler.cs`、`Assets/YC/Application/Gameplay/ExploreLocationCommandHandler.cs`、`Assets/YC/Application/Gameplay/MoveCityCommandHandler.cs` 为准：入场、探索、移动城市三条事件结算链路已改为优先读取统一卡牌会话；探索和移动城市打开待选时不再额外手写旧待选状态，统一由 `CardFlowStateAdapter.OpenPendingSession()` 双写。
- 以 `Assets/YC/Tests/EditMode/SetupCommandHandlerTests.cs`、`Assets/YC/Tests/EditMode/ExploreLocationCommandHandlerTests.cs`、`Assets/YC/Tests/EditMode/MoveCityCommandHandlerTests.cs` 与 `Assets/YC/Tests/EditMode/PresentationSelectionControllerTests.cs` 为准：已补最小回归覆盖入场事件、探索事件、移动城市事件的待选打开与清掉旧 `PendingChoiceState` 后仍可结算，并补快照优先级测试；本次验证结果为 Setup 15/15、Explore 26/26、MoveCity 14/14、PresentationSelection 6/6 全部通过。
- 当前兼容边界：`PendingChoiceState` 仍由适配器双写，`GameState.HasPendingChoice()` 仍同时识别两种待选状态；旧状态只用于存量兼容，不应再新增以旧状态为主事实源的 UI/结算逻辑。

## 2026-06-29 城市样式宣告闭环
- 以 `Assets/YC/Domain/CityStyles/DeclareCityStyleService.cs`、`Assets/YC/Domain/CityStyles/CityStylePatternMatcher.cs`、`Assets/YC/Application/Gameplay/DeclareCityStyleCommandHandler.cs` 为准：城市样式宣告已从计分占位推进到 Domain / Application 闭环，支持样式存在校验、重复宣告拒绝、影响力供应校验、基于已建设施、设施类型与城市面板槽位的基础条件匹配，并将正式宣告写回 `GameState`。
- 以 `Assets/YC/Presentation/CityStyleSelectionController.cs`、`Assets/YC/Presentation/EventChoiceDialog.cs`、`Assets/YC/Presentation/MobileCityInteractionController.cs` 为准：`宣告样式` 按钮已接通 UI 弹窗，可展示可宣告样式、不可宣告原因并提交宣告命令。
- 以 `Assets/YC/Tests/EditMode/DeclareCityStyleCommandHandlerTests.cs` 与 `Assets/YC/Tests/EditMode/PresentationSelectionControllerTests.cs` 为准：已补成功宣告、条件不足、重复宣告、最终计分加入样式分和最小表现层选择控制器测试。
- 当前边界：样式数据仍是最小定义；设施 180 度旋转占用已在 `CityStyleDeclarationState` 中记录使用槽位，但视觉旋转、复杂旋转/翻转匹配和特殊行动联动尚未实现。

## 2026-06-27 事实源文档校准

- 以 `Assets/YC/Application/Gameplay/BuildFacilityCommandHandler.cs`、`Assets/YC/Domain/Facilities/BuildFacilityService.cs`、`Assets/YC/Presentation/BuildFacilitySelectionController.cs` 为准：建设已经形成规则层与表现层闭环，不再应被记为“未实现”。
- 以 `Assets/YC/Domain/Scoring/FinalScoringService.cs`、`Assets/YC/Domain/CityStyles/CityStyleDatabase.cs`、`Assets/YC/Tests/EditMode/FinalScoringServiceTests.cs` 为准：最终计分当前结算基础分、区控分、资源分、设施分、城市样式分与平局判定；设施分会从已建设施牌面分拆出，城市样式分按正式宣告记录结算。
- 以 `Assets/YC/Application/Sessions/LocalhostAutoplayRunner.cs`、`Assets/YC/Tests/EditMode/LocalhostAutoplayRunnerTests.cs` 为准：自动跑局当前覆盖固定四席位、固定初始点、入场默认选项、显式探索选项、城市移动、一次调度、建设或部署主行动、当前城市资源点采集、至少一条显式付费航道采集、至少一次显式指定对手为航道路费接收方的采集、收尾推进与最终计分；仍不覆盖特殊行动。
- 以 `Assets/YC/Presentation/MobileCityInteractionController.cs`、`Assets/YC/Presentation/GameSettingsMenuController.cs` 为准：`建设` 和 `宣告样式` 按钮已接通；`使用角色牌`、`特殊行动` 按钮仍是占位提示；设置菜单主体区域仍为空白占位。

本文记录当前代码已经落地的状态。按日期追溯请看 `docs/工作记录/README.md`。

## 已完成

### 规则与阶段

- `Setup -> Entrance -> RoundStart -> CharacterCover -> ActionRound1 -> ActionRound2 -> ResourceCollection -> Cleanup -> FinalScoring/GameOver` 固定阶段流已经接通。
- 四人地图结构化数据、路线搜索、路费计算、红区开放限制和区控统计已经落地。
- 入场阶段支持起始玩家、初始地块、初始资金与入场默认选项解析。

### 主要行动与结算

- 已接通 `DeployInfluence`、`DispatchInfluence`、`ExploreLocation`、`MoveCity`、`BuildFacility`、`CollectResource`、`EndAction`。
- 建设支持设施供应校验、唯一设施限制、自动寻找首个城市面板空槽、资源/金券支付、建成奖励、即时得分与设施放置记录。
- 采集支持显式 `routeIds`、共享航道路费、接收方指定和“本阶段新获得金券不能支付本阶段路费”限制。
- `EndActionCommandHandler` 在回合推进进入 `FinalScoring` 时会调用 `FinalScoringService` 立即写回最终结算。
- 最终计分当前结算：
  - `BaseScore`
  - `RegionScore`
  - `ResourceScore`
  - `FacilityScore`
  - `CityStyleScore`
  - 平局时先比剩余金券，再比剩余至纯源石

### 表现层

- 开始页、四人地图显示、信息面板、顶部提示条、规则书入口、返回主菜单确认都已可用。
- 右下角行动面板已接通 `建设` 弹窗，允许选择设施和支付方式。
- 采集阶段 UI 已支持高亮可采集资源点、点击航道支付或切换接收方、提交采集。
- 事件选择弹窗当前优先读取 `PendingCardSessionState`，可处理入场事件、探索事件、移动城市事件的待选展示、影响力目标选择与选项提交；旧 `PendingChoiceState` 只保留兼容 fallback。
- `宣告样式` 已接通选择弹窗与命令提交；`使用角色牌`、`特殊行动` 目前只显示“暂未实现”提示。
- 设置菜单齿轮、`规则书` 按钮、`返回主菜单` 按钮可用，但设置菜单主体区域仍为空白占位。

### 联机与开发验证

- Unity 官方联网栈方向的多人房间、Host 权威命令传输和状态快照同步 MVP 已接入。
- `LocalhostAutoplayRunner` 当前覆盖：
  - 四席位 Host 权威命令链
  - 固定初始选点与入场默认选项
  - 行动阶段会优先补足一次显式探索、一次城市移动和一次调度，再继续建设或部署
  - 探索会提交明确事件选项和明确路径参数，避免只依赖默认路线/默认选项
  - 调度会提交真实 `DispatchInfluence` 命令，将已有影响力移动到可作为后续采集路费接收证据的航道槽
  - 采集阶段为各玩家提交采集命令，并至少覆盖一次显式付费航道采集和一次显式指定对手为航道路费接收方的采集
  - 收尾推进到 `FinalScoring`
  - 快照输出探索地点、移动路线、调度路线、付费采集路线、指定接收方采集证据、设施、影响力、采集、最终计分摘要
  - 失败时输出最后命令、待选择状态、待处理卡牌会话和最近日志
- 自动跑局当前不覆盖：
  - 特殊行动

### 测试

- EditMode 已覆盖地图、路径、费用、影响力、探索、移动城市、建设、采集、最终计分、联网和自动跑局。
- 当前关键用例包括：
  - `BuildFacilityCommandHandlerTests`
  - `ResourceCollectionCommandHandlerTests`
  - `ResourceCollectionServiceTests`
  - `FinalScoringServiceTests`
  - `LocalhostAutoplayRunnerTests`
  - `MultiplayerLaunchRegressionTests`
  - `PresentationSelectionControllerTests`
  - `SetupCommandHandlerTests`
  - `ExploreLocationCommandHandlerTests`
  - `MoveCityCommandHandlerTests`

## 当前限制

- 三人地图规则数据仍是 placeholder。
- 入场事件已可通过通用事件选择弹窗处理；多人依次入场选择体验、日志详情、回放、存档尚未完成。
- `使用角色牌`、`特殊行动` 规则与 UI 仍未接通。
- 角色牌、设施入场、企业特效等统一效果系统尚未实现完毕；城市样式当前只有最小样式定义和宣告闭环。
- 城市样式宣告已接通正式宣告、基础匹配、状态写回和最终计分；全量样式数据、设施旋转占用视觉化、复杂旋转/翻转匹配和特殊行动联动仍待扩展。
- `MobileCityInteractionController` 仍承担较多原型职责，后续仍需拆分。

## 下一步建议

1. 补齐 `使用角色牌`、`特殊行动` 的规则与 UI 闭环。
2. 扩展城市样式定义数据，补齐真实牌面匹配、设施旋转占用视觉化和特殊行动联动。
3. 扩展自动跑局，继续覆盖特殊行动，以及更多带事件影响力目标和不同路费接收方组合的分支。
4. 补齐入场事件和多人入场顺序 UI。
5. 录入三人地图真实数据。
