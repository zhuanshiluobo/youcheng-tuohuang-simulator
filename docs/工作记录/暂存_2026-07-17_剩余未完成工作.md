# 剩余未完成工作暂存

> 文档性质：2026-07-17 工作区暂存清单，不是当前规则或实现事实源。完成其中事项后，应把结论更新到 `docs/概览/项目总览.md`、正式计划与工作记录，并删除或归档本暂存清单。

## 已完成，不再重复实施

- 延伸枢纽三张备用设施牌已经与公共建设牌统一为 `99×141` 卡框和 `3px` 卡图内边距。
- 延伸枢纽与普通建设牌已经共用 `CardPointerInteraction`、`FacilityCardDragUtility`、拖动虚影、取消生命周期和 `CityBoardSlotDropTarget` 落点解析。
- 延伸枢纽拖动期间已经复用建设面板的合法空槽高亮，落下时通过 `BuildFacilityService.ValidateReserveBuild()` 即时校验，并通过 `BuildReserveForFree()` 统一结算。
- 简陋工程营的额外建设继续复用普通 `BuildFacilityService.Build()`；载具仓库探索复用受授予探索入口和普通探索表现流程；设施贸易与角色牌出售复用 `ResourceSaleService`；免费移动复用受授予移动入口。
- 五张基础角色牌当前定义的策略与计谋均已接入；未知牌或扩展牌的 `Unsupported` 防御分支不代表基础角色牌仍未完成。

## P0：城市样式特殊行动闭环

### 1. 冻结五种特殊行动规则

按 `docs/计划/2026-07_下一阶段完整计划.md` 的规则冻结表，逐项明确费用、目标、次数、执行顺序、取消语义、部件不足与中途目标失效处理：

- 军工化区域：确认目标范围、可放置数量计算区域，以及玩家影响力不足时是否部分执行；牌面上限为 3。
- 动员配套体系：替换一个对手玩家影响力，复用既有替换、移动城市阻挡和供应不足规则。
- 复合动力系统：支付 1 个源石碎片及合计 3 个源岩/异铁，免费移动城市后在经过航道放置影响力；明确组合支付和经过航道选择。
- 源石工业中枢：支付 6 金券获得两次额外主要行动；明确行动预算、取消/失败返还、快速行动窗口和角色牌限制。
- 高效移动管理体系：支付 3 个源石碎片连续执行两次免费移动城市；明确第二次无合法目标及事件链中断处理。
- 物资中继站保持宣告即时奖励，不生成特殊行动入口。

验收标准：规则矩阵不再含“待核定”字段，并与规则书、牌图、术语表一致。

### 2. 领域、命令和合法查询

- 实现并注册 `UseSpecialAction` 命令处理器和正式领域服务。
- 建立特殊行动定义、费用、目标、次数、禁用原因及必要待选状态的数据契约。
- 复用现有支付、影响力、移动城市、事件和目标查询服务；失败时不得留下部分状态。
- 按特殊行动 ID 追踪每回合使用情况，避免把“每种每回合一次”错误实现成“所有特殊行动合计一次”。

验收标准：每种行动覆盖成功、资源不足、非法目标、目标失效、部件不足、取消和失败不写回测试。

### 3. 标记生命周期与收尾复位

- I 级样式标记使用后进入“已使用”，收尾时回到“未使用”。
- II 级样式标记按牌面次数格推进，收尾时按规则向下移动。
- 统一 `UnlockedSpecialActionId`、`RemainingSpecialActionUses` 与 `UsedSpecialActionIdsThisRound` 的状态语义。
- 行动面板只要存在任一合法特殊行动就应可用，并展示每项具体禁用原因。

### 4. 表现层与交互

- 替换 `MobileCityInteractionController.OnSpecialActionClicked()` 的“暂未实现”提示。
- 展示已解锁行动、费用、剩余次数和禁用原因。
- 地图目标、航道、移动城市和影响力选择复用已有协调器；不要在表现层直接修改 `GameState`。
- Esc、右键和取消必须在提交前不消耗费用、次数或主要行动。

### 5. 联机、自动跑局和测试

- 确保特殊行动命令、待选状态和额外行动预算能由 Host 权威处理并随快照同步。
- `LocalhostAutoplayRunner` 至少真实执行一种 I 级和一种 II 级特殊行动，然后继续推进到最终计分。
- 补充领域、命令、表现层、联网重放、自动跑局和完整 EditMode 回归。

## P1：当前工作区交付验证

- 重新生成 Localhost 构建；现有构建产物早于本轮延伸枢纽、角色牌、设施复用和样式宣告改动。
- 人工冒烟检查延伸枢纽拖拽、跨 Canvas 虚影层级、非法落点反馈、设施待选、角色牌结算和样式宣告。
- 更新 `docs/概览/项目总览.md` 中已经过期的测试基线，并同步当前工作总结与文档索引。
- 有条件时执行 Editor Host + 两个 Development Build Client 的三进程验收，以及多个 Steam 账号的 P2P/Relay 验收。

## P2：明确延期范围

- 三人地图真实规则数据及三人专属事件裁剪。
- 存档、完整回放、日志详情、断线对局恢复和按玩家裁剪的私有联网快照。
- 设置菜单的音量、画面等通用设置。
- 企业扩展角色、企业、公路、扩展城市样式与扩展事件。
- 正式美术替换和整体 UI 重制。

## 可选架构优化

- 将 `BuildInfoPanel.TryLoadFacilityCardTexture()` 迁移到独立卡牌资源加载器，解除弹窗对具体面板类的依赖。
- 向设施效果协调器注入备用设施合法查询服务，替代内部直接创建 `BuildFacilityService`。
- 在接入特殊行动前抽取通用地图高亮和目标选择构造器，避免设施、角色牌和特殊行动重复遍历地图。
- 后续按功能拆分体积较大的 `CharacterCardService` 与 `ExplorationEventPresenter`；当前没有由此导致的已知功能缺陷，不与 P0 混做。

## 当前验证基线

- 建设面板定向 EditMode：`19/19 Passed`。
- 本次审计重跑完整 EditMode：`593/593 Passed`。
- 结果文件：`Logs/EditModeTests/editmode-20260717_133606.xml`。
- 尚未重新生成 Localhost 构建，也尚未执行本轮人工 UI 冒烟；这两项仍按 P1 保留。
