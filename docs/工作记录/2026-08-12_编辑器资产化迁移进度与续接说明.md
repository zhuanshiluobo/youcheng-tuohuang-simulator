# 编辑器资产化收口与后续交接说明

本文记录 2026-08-05 至 2026-08-12 期间，由 Codex 通过单一 Unity Editor/MCP 实例推进的编辑器资产化工作，以及 2026-08-12 完成的次级布局资产化与单机验收。本文不替代规则、玩法和联机专题文档；判断最终完成度时仍应以当前代码、最新测试和最终证据为准。

## 0. 先读结论

- 用户最初列出的最高等级资产化问题已经全部完成，最终总审核结论为完成度 `100%`、P0 `0`。
- 用户已重新授权并完成 Action、Card、Zoom 三组共 `47` 个次级固定布局候选；`12` 个动态构造全部保留。
- 次级布局资产化完成时，`Presentation` 的真正 `new Vector2(...)` 构造为 `87`，相对批前 `134` 减少 `47`；后续将只供 Editor Builder 使用的 UGUI 工具移出运行时目录后，当前统计为 `81`。两者都不是后续清零目标。
- 本轮新增 `ActionPanelLayoutProfile.asset`、`CardInteractionLayoutProfile.asset`、`ZoomableViewerLayoutProfile.asset`，均由 Builder 创建并显式接入 Prefab，由 Readiness、负向合同和静态扫描共同守门。
- 最新完整验证为 EditMode `999/999`、PlayMode `2/2`，失败和跳过均为 `0`；最终 Development Build 成功，构建过程 Console error `0`，自动跑局退出码 `0` 并进入最终计分。
- 三档分辨率与三档缩放的九组 Development Player 截图已重采集，九份运行日志 error/exception 均为 `0`。
- 当前成果已按生产实现、验收工具、交接文档三层固化为可回退基线；原始截图、日志、构建目录和 Blender 备份文件不进入 Git。

## 1. 工作方法与约束

- 按重要性和逻辑相关性分批迁移，不机械转换所有常量。
- 实现工作与独立审核分离：实现代理完成后停止修改，由只读审核代理重新读取资产、场景、测试和真实 Play 证据。
- 根代理负责唯一 Unity 实例、工作树冲突、场景与 Prefab GUID、测试门禁和最终完成度判断。
- Prefab、Scene 和 ScriptableObject 接线统一通过 Unity API 或 Editor Builder 完成，不手写 Unity YAML。
- 不启动第二个 Unity Editor，不提交或推送未经用户明确授权的改动，不处理范围外脏文件。
- `Temp/UnityLockfile` 不单独作为项目占用依据；不得批量或递归删除文件。

## 2. 已有完整证据的收口范围

### 2.1 主 UI、HUD、弹窗和场景结构

开始菜单、设置菜单、Gameplay HUD、行动面板、信息面板、建设信息面板、回合栏、规则书/图片查看器、事件与效果弹窗等主要界面已经迁入 Prefab、View 组件、Registry 和 Editor Builder 体系。运行时代码不再负责重建这些完整界面层级。

第一阶段的准确边界是：

> 主 UI、HUD、弹窗、场景结构以及地图 Prefab 迁移完成；这不等于整个项目的所有主题、布局、内容数据库、网络基础设施和表现资源均已完成资产化。

### 2.2 地图 Prefab 与场景残留收口

- `Assets/YC/Presentation/Prefabs/Map/MapView.prefab` 曾存在 22 个丢失脚本引用。根因是 `MapHotspot` 从旧多类脚本迁到同名脚本后，Prefab 仍保存旧脚本 GUID。
- 重建后 22 个 `MapHotspot` 均恢复，丢失脚本数由 22 降为 0；Prefab GUID 保持 `8b0f4d6ff3b02384e96723a98fa9fa0d`。
- 地图 `SpriteRenderer`、`MapDisplayController`、`MapCoordinateSpace` 和 `MapView` 已统一位于 `MapView.prefab` 根对象。
- `SampleScene/MapRoot` 删除了重复的地图显示组件，只保留 `Transform` 与 `MobileCityInteractionController`；其唯一子对象仍是连接状态的 `MapView` Prefab 实例。
- `MapViewEditorAssetBuilder` 和 `MapViewEditorAssetTests` 已增加未知组件、残留组件、丢失脚本、Prefab 连接和序列化引用门禁。

当时的验证证据：

- 地图定向 EditMode：`3/3`。
- 完整 EditMode：`894/894`，任务 `8ac46303cbe14cc9bc583bde3904c7ed`。
- 真实 `SampleScene` Play：`MapViewPlayModeProbe`、`RoundTrackerPrefabPlayModeProbe`、`GameplayInteractionHudPlayModeProbe` 全部通过。
- Play 退出后场景 `roots=6`、`dirty=false`、Prefab connected、构建器资产校验与场景校验均为 0 错误，Console 为 0 error/0 warning。

### 2.3 卡图与设施目录

- `CardVisualCatalog.asset` 集中保存设施 46、城市样式 6、角色正面 5、角色背面 4、城市板 1，共 62 个持久化引用；HUD 与弹窗通过 `GameplayDialogRegistry` 等显式入口读取。
- 已移除目标消费者中的运行时图片路径发现、文件 IO 和 `Resources.Load<Texture2D>` 链。
- `FacilityCardCatalog.asset` 保存 46 个设施定义，并由 `FacilityCatalogBootstrap` 注入纯 Domain 数据库。
- `FacilityCardDatabase` 在 Bootstrap 注入前明确失败，不把“未初始化”伪装成“查无此卡”；旧 IO、JSON、Legacy、fallback 和名称推断链已经移除。
- 设施目录增加 effectId 白名单、入场关键词一致性、建成奖励一致性、深拷贝和重复初始化冲突门禁。

当时的设施目录独立审核包括定向 `17/17`、完整 EditMode `894/894`、冷启动 PlayMode `1/1` 和真实 Start→Sample Play。上述数字是当时工作区证据，后续新增测试后应重新建立总数基线。

## 3. 已完成并通过审核的资产化迁移

以下内容已经完成实现、持久资产接线、Unity MCP 验证和独立审核，不应再描述为“尚未开始”或“只完成静态盘点”。

### 3.1 UI 主题与高价值布局

- `UiTheme` 已变为严格读取 `UiThemeCatalog` 的门面，未初始化时明确抛错。
- `UiThemeCatalog.asset` 与 `UiThemeBootstrap` 已存在；颜色、玩家色、Canvas 基准分辨率和少量跨界面共享尺寸已进入持久化资产。
- 已迁移的高价值共享布局包括 Canvas `1920×1080`、match `0.5`、对话框操作按钮 `220×48`、折叠地图提示 `650×260`、查看器关闭按钮 `42×42` 及偏移 `(-18,-12)`。
- 还存在独立布局资产：`EffectDialogLayoutProfile.asset`、`EventChoiceDialogLayoutProfile.asset`、`ExpandableInfoPanelLayoutProfile.asset` 和 `CardBoardVisualLayout.asset`。
- `Presentation` 中 `new Vector2(...)` 从原始快照的 301 个表达式降为本轮最终静态扫描的 `87` 个。剩余项包含运行时动态 UI 计算、地图/游戏坐标和通用 helper，不能把 `87` 整体视为应资产化债务。
- EventChoiceDialog、Effect/Facility 对话框、ExpandableInfoPanel、卡板/地图空间等高价值固定布局已经迁入 Profile 或持久布局资产，并有 Builder、Readiness 与负向合同测试。

### 3.2 次级布局资产化

三组次级布局已经在不修改领域规则、命令接口、Mirror 快照和存档模型的边界内完成：

| 工作簇 | 批前 | 批后 | 已迁移固定项 | 保留动态项 |
|---|---:|---:|---:|---:|
| ActionPanel / Prompt | `9` | `2` | `7` | `2` |
| BuildInfo / CityStyle / CardPointer | `33` | `4` | `29` | `4` |
| Zoomable Viewer | `17` | `6` | `11` | `6` |
| `Presentation` 全局（资产化阶段） | `134` | `87` | `47` | 本轮锁定的 `12` 项全部保留 |

- ActionPanel 固定容器布局进入 `ActionPanelLayoutProfile`；Prompt 的文本测量尺寸和动态高度仍在代码中。
- BuildInfo、CityStyleDeclaration 和 CardPointer 固定布局进入 `CardInteractionLayoutProfile`。
- `CardDragGhostLayout` 由 BuildInfo、城市样式特殊行动和 `FacilityEffectChoiceDialog` 共享；`FacilityCardDragUtility.CreateDragGhost(...)` 显式接收该布局，没有第二套 fallback。
- Zoomable Viewer 的收起/展开按钮固定布局与 `1400×860` 回退进入 `ZoomableViewerLayoutProfile`；纹理比例、Viewport、缩放和面板适配仍为动态计算。
- 未恢复已撤销的 `GameplayInteractionLayoutProfile`，也未引入运行时资源发现或全局静态 fallback。
- 后续 Editor 边界清理将 `UguiUtility.cs` 连同原脚本 GUID `4c7dbd9adf5294c48a79e493e3025b1e` 移至 `Assets/YC/Editor/UguiEditorUtility.cs`；原工具中的 6 个固定 `Vector2` 因此离开 `Presentation` 扫描范围，当前全局计数为 `81`，并非再次迁移或删除动态项。

固定资产标识如下：

| 资产 | GUID | SHA-256 |
|---|---|---|
| `ActionPanelLayoutProfile.asset` | `fdfb2e039f0c842439596ccfcea7686b` | `6492D1B6989EF40F778C37651ECE04AA2E46656A5DA331A7E2EA3818A46BEFEC` |
| `CardInteractionLayoutProfile.asset` | `84c89edc1445da24cb6d911a4839e1f3` | `512CB805A30118FDA68CDA0BBE5A98C0EEAC48F1E74801C0E7E5CE311C9C76B4` |
| `ZoomableViewerLayoutProfile.asset` | `6897c32684bc9b7448d53ba522fcdbc8` | `96CD5431E74AA7525EF57CD97458E6FC6C121F19DF0DD772612B17B07FE4A5C2` |

锁定清单 `Assets/YC/Editor/Data/secondary_layout_manifest.json` 的 GUID 为 `84181727001c7bc48b3b310efcce7325`，SHA-256 为 `EFCBE8FFF20617DC67A9671AEFE123A83553D739BBA7B09476B3964869213544`。

### 3.3 内容数据库

当前 `Assets/YC/Presentation/Content` 已包含：

- `EventCharacterCardCatalog.asset`
- `CityStyleSpecialActionCatalog.asset`
- `FacilityCardCatalog.asset`
- `CardVisualCatalog.asset`
- `UiThemeCatalog.asset`

其中 Event 22、Character 5、CityStyle 6、SpecialAction 5 已由联合 Catalog 与生产 Bootstrap 注入对应领域数据库；未初始化访问会 fail-fast，不再由领域数据库生成生产事实。对应 Builder、Build Readiness、Prefab/场景接线和 EditMode 合同已经通过独立审核。

### 3.4 网络基础设施

- `Assets/YC/Presentation/Prefabs/Infrastructure/NetworkRuntimeRoot.prefab` 已存在。
- 当前静态扫描中，`NetworkManager`、本地/Steam Transport 与 `SteamBootstrap` 的 `AddComponent` 命中只出现在 `NetworkRuntimeEditorAssetBuilder.cs`，生产运行时代码未命中同类动态创建。
- `NetworkRuntimeBuildReadiness` 会检查核心组件唯一性、启用状态、Transport 选择、执行顺序、Prefab GUID/localID、场景连接和非法 override。
- `NetworkRuntimeEditorAssetTests`、Prefab/场景 YAML 门禁和生命周期合同已经通过；资产化范围已经完成。真实本地多实例、Steam P2P/Relay 和构建产物属于人工交付验收，不应误报为“网络 Prefab 尚未资产化”。

### 3.5 表现资源

先前 `Material`、`SpriteAtlas`、`AnimationClip`、`AnimatorController` 均为 0 的结论已经过时。当前资产总数为 Material 2、SpriteAtlas 1、AnimationClip 1、AnimatorController 1。资产包括：

- `Assets/YC/Presentation/Materials/MapFeedbackAdditive.mat`
- `Assets/YC/Presentation/Materials/MapPiecePlayerColor.mat`
- `Assets/YC/Presentation/Sprites/Map/MapVisuals.spriteatlas`
- `Assets/YC/Presentation/Animations/MapPlacementFeedback.anim`
- `Assets/YC/Presentation/Animations/MapPlacementFeedback.controller`

这些资产用于地图反馈和地图棋子表现，正式 Prefab 已接线；不应为了增加资产数量继续盲目生成资源。

## 4. 完成证据与当前快照

- Unity 版本：`2022.3.62f2c1`。续接时应先发现当前唯一 MCP 实例，不要硬编码旧实例哈希，也不要启动第二个 Unity Editor。
- 当前分支：`codex/special-actions-and-interaction-fixes`。生产实现提交为 `2a98bdf`（`完成桌面导航、3D表现与次级布局资产化`），验收工具与忽略规则提交为 `4454f3b`（`补充 Development Player 验收工具与本地产物忽略规则`）；均仅保存在本地，未推送远端。
- 本轮次级布局定向测试：`SecondaryLayoutEditorAssetTests` `3/3`；相关 Effect/Event/Secondary 组合定向测试 `27/27`。
- 基线提交前完整 EditMode：`999/999`，失败 `0`、跳过 `0`，任务 `d44ae909506b4a17bea0108e5088df69`；完整 PlayMode：`2/2`，失败 `0`、跳过 `0`，任务 `7028d2798e074b409572c443f7731684`。此前记录的 `03916e...`、`75caf1...` 仅为历史任务，不再作为最新基线。
- 精确工具提交 `4454f3b` 上，Windows Development Player 已通过 `BuildLocalhost.ps1 -Development` 重建；构建退出码 `0`、结果 `Success`，编译错误、缺失脚本和构建异常均为 `0`。
- 自动跑局退出码 `0`，日志记录 `Success: True`、`Phase: FinalScoring`、`FinalScoringResolved: True`、胜者 `P3`。
- `Artifacts/BaselineAcceptance/ZoomMatrix` 保存 `1920×1080`、`1600×900`、`1280×720` × `90% / 100% / 200%` 的九组截图与九份独立日志。
- `info-panel-smoke.png`、`right-card-smoke-buildfocus.png`、`right-card-smoke-declare.png` 分别证明信息面板、BuildFocus 建设面板和 Declare 城市样式预览可在最终 Development Player 中打开。旧信息面板脚本按主菜单坐标点击的问题已在 `4454f3b` 修复，不把无效主菜单截图计入验收。
- 构建、自动跑局、三组烟测和九宫格共 14 份日志已扫描：`LogError`、未处理 Exception、缺失脚本和 RoundTracker 配置错误均为 `0`。原始证据只保存在被忽略的 `Artifacts/`，Git 文档只记录命令、提交、退出码和结论。
- UGUI Editor 边界清理后的完整 EditMode 为 `1000/1000`，失败 `0`、跳过 `0`，任务 `cb46c52870714b308eb63533bbd07001`；完整 PlayMode 为 `2/2`，失败 `0`、跳过 `0`，任务 `ac60860b8e264f90ba5d02a2d07edbd0`；清空后 Console error 为 `0`。
- 边界清理后的 Development Player 再次构建成功，自动跑局退出码 `0` 且最终计分完成；BuildFocus、Declare、信息面板代表性烟测及五份构建/Player 日志错误命中均为 `0`。证据位于被忽略的 `Artifacts/UguiBoundaryAcceptance/`。
- 城市样式预览 Prefab 清理前后 SHA-256 均为 `5801438C8B969C1FC9560D104C4D397839CA53AB04BBAA2B08F9AA429E8BD528`，GUID 仍为 `8b2296b0534cbde498ff279f40283ff0`，关闭按钮 localID 仍为 `4046547580711502928`；最终 Declare 截图中的关闭按钮与布局保持一致。
- 为消除最终构建缺失脚本错误，已移除孤立且无消费者的 `Assets/Resources/UnityPlayerAccountSettings.asset` 及其 `.meta`。该资产引用的脚本 GUID 在当前 Assets、Packages 和 PackageCache 中不存在，项目也未安装 Player Accounts 包。
- 最终只读审核重新核对了资产、GUID、Prefab/场景接线、静态构造数、测试、Player 日志和构建证据，结论 P0 `0`、P1 `0`。

## 5. 本轮验收结论与剩余边界

### 5.1 已完成验收

- 3D 地图与平面 HUD 在九组窗口/缩放组合下保持分层和可读性；90%、100%、200% 显示范围有明确差异，HUD 不随地图缩放。
- 运行版首次刷新不再依赖场景根对象 `Awake` 顺序；`RoundTrackerController` 改为幂等按需初始化，并增加 `RefreshFromState_BeforeAwake_InitializesOnDemand` 回归测试。
- 建设信息面板和城市样式预览已在最终 Development Player 中打开并留存截图。
- Card Drag Ghost 的三处共享消费者、Viewer 固定/动态边界和缺失 Profile fail-fast 均由静态门禁、Readiness 与完整测试覆盖。
- 构建过程、九份可视 Player 日志和自动跑局结果均没有本轮运行时 error；无头自动跑局的 `-nographics` Shader unsupported 信息属于无图形设备路径，不出现在可视验收日志中。

### 5.2 仍需真人输入手感复核的项目

- 当前自动化与截图可以验证布局、接线、状态和日志，但不能替代真人对鼠标手感的主观确认。发布前仍建议由用户在最终构建中亲手复核：右键跨 HUD 持续拖动、HUD 按下不启动拖动、光标锚点缩放、卡牌拖拽落点、Viewer 拖动/翻页/收起/Esc。
- 这项剩余复核不授权也不要求新增产品功能；若发现异常，应按具体复现单独修复。

### 5.3 明确排除范围

- 三人地图、入场扩展、日志、存档、回放、设置、扩展角色、正式 UI、联机验收和任何产品功能均不属于本轮。
- 不以资产化阶段的 `87` 或 Editor 边界清理后的 `81` 为由继续迁移动态测量、纹理比例、Viewport 适配、地图坐标或数学向量。
- 不恢复运行时资源发现、全局静态 fallback、动态 UI 壳创建或已撤销的 `GameplayInteractionLayoutProfile`。


## 6. 不应误判的事项

- 不要把所有 `new Vector2` 都迁入资产；动态测量、插值、地图坐标和数学向量应留在代码中。
- 最高等级资产化已经通过持久化资产、Bootstrap、场景/Prefab 接线、Unity 测试和独立审核；不要因历史文档仍有旧路径描述而恢复 `StreamingAssets`、运行时资源发现或动态网络对象创建方案。
- 不要用 Unity MCP 的陈旧 `is_changing` 字段单独判断卡死；同时核对 `Application.isPlaying`、编译状态、工具响应和 Editor 日志。
- PlayMode 中修改脚本会触发域重载并中断 MCP 会话。应先退出 Play，再刷新和编译。
- 历史工作记录中的 `StreamingAssets`、运行时 `Resources`、动态网络对象创建和资产数量描述可能已被本轮迁移替代，引用前必须核对当前代码。
- `Temp/UnityLockfile` 不能单独证明项目被占用；删除前必须先确认没有命令行包含当前项目路径的 Unity 进程。
- 禁止递归或批量删除。工作树很脏，续接时不得用 `git reset --hard`、`git checkout --` 或同类命令覆盖现有改动。

## 7. 给其他对话的启动清单

1. 先完整阅读根目录 `AGENTS.md`、`docs/概览/项目总览.md` 和本文。
2. 运行只读 `git status --short`，确认当前改动范围；不要把已有改动重新归属给新对话。
3. Action/Card/Zoom 的 `47` 个固定候选已经完成；不要重复迁移，也不要触碰明确保留的 `12` 个动态项。
4. 若未来继续调整布局，先重新统计目标文件的 `new\s+Vector2\s*\(`，锁定固定/动态分类和批前资产哈希，再实施最小逻辑批次。
5. Unity 操作只由一个根对话串行执行；重建后先跑定向测试，再跑完整 EditMode、PlayMode 和 Console 检查。
6. 新增或修改 Prefab/Scene 时必须保留 GUID/localID、Build Readiness、冷启动和场景 override/removal 门禁；不要手写 Unity YAML。
7. 延续当前 Profile 时必须显式序列化引用并 fail-fast，不得增加运行时发现或代码默认值。

可直接交给下一对话的范围说明：

> 最高等级和 Action/Card/Zoom 次级布局资产化均已完成；资产化阶段 `Presentation` 静态数为 `87`，UGUI Editor 边界清理后为 `81`，本轮锁定的 `12` 个动态项必须保留。先阅读 `docs/工作记录/2026-08-12_编辑器资产化迁移进度与续接说明.md`，不要重复实现三份 Profile，也不要把当前统计作为清零目标。保留当前工作树，不启动第二个 Unity，不批量删除，不暂存、提交或推送未经授权的改动。
