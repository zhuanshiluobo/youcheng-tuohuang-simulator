# 2026-07-08 计划与模块文档完成度审计记录

## 审计范围

- `docs/计划`
- `docs/模块文档`
- `docs/建设牌输出`
- `docs/城市样式牌输出`
- 当前代码事实源：`Assets/YC/Domain`、`Assets/YC/Application`、`Assets/YC/Presentation`、`Assets/YC/Tests/EditMode`

## 已完成或已由后续记录覆盖

### 早期计划

- `docs/计划/00_模拟器总体目标.md`：项目定位、MVP 范围和当时进度已统一迁入当前的 `docs/概览/项目总览.md`；早期计划文档已于后续文档清理中删除。
- `docs/计划/01_MVP规则闭环计划.md`：基础阶段流、主要命令、采集、收尾和最终计分已接通；剩余未完成项见下方“未完成清单”。
- `docs/计划/03_数据与素材整理计划.md`：四人地图、建设牌正式数据、城市样式 6 张牌面数据和牌图资源已阶段性落地；三人地图与角色牌等仍未完成。
- `docs/计划/04_测试与验收计划.md`：EditMode 测试体系、命令处理测试、最终计分测试、本地联机自动跑局和多项表现层回归已建立；特殊行动、存档/回放和端到端联网仍未完全覆盖。

### 模块文档

- `docs/模块文档/影响力模块实现规格书.md`：部署、调度、放置、移动、移除、区控统计和相关命令处理已落地；角色牌和企业扩展带来的额外影响力效果仍待统一效果系统接入。
- `docs/模块文档/探索模块需求文档.md`：探索命令、路线费用、资源指示物、事件待选和表现层入口已落地；更复杂事件效果仍依赖后续统一效果系统。
- `docs/模块文档/航道位置配置与显示模块文档.md`：四人地图航道槽位坐标、显示、校验和影响力占用刷新已落地；三人地图显示配置仍待真实规则数据。
- `docs/模块文档/通用抽卡流程模块接口说明.md`：`CardFlowService + IEventCardScenario + PendingCardSessionState` 已接入探索、移动城市和入场事件；旧 `PendingChoiceState` 仅保留兼容 fallback。
- `docs/模块文档/联网命令重放牌堆同步说明.md`：Host 权威快照同步 MVP 已落地；断线重连、状态版本校验和增量同步仍未完成。
- `docs/模块文档/对局界面控制器职责拆分说明.md`：第一轮 Presentation 拆分已完成，后续仍需继续拆出地图点击状态机等更细边界。

## 素材与牌面输出状态

- `docs/建设牌输出`：不能删除。`FacilityCardDatabase` 当前仍优先读取 `docs/建设牌输出/building_cards_manifest.json`，并生成指向 `docs/建设牌输出/建筑卡片` 与 `docs/建设牌输出/备用卡片` 的 `ImageRelativePath`。
- `Assets/YC/Presentation/Resources/CardImages/Facilities`：已有 12 张代表性设施牌图资源，但不足以替代 `docs/建设牌输出` 的 41 张正式建设牌截图和 manifest。
- `docs/城市样式牌输出`：6 张城市样式牌图已复制到 `Assets/YC/Presentation/Resources/CardImages/CityStyles`，运行时 `CityStyleDatabase` 已硬编码 6 张样式的分数、匹配条件、宣告次数和图片路径。该目录可视为阶段性可清理候选，但仍是素材拆分来源和 manifest 对照源。

## 未完成清单

1. `使用角色牌` 和 `特殊行动` 的规则、UI、测试和自动跑局覆盖仍未接通。
2. 三人地图真实规则数据与对应航道/资源点/显示坐标仍未录入。
3. 入场事件仍缺多人依次选择体验、日志详情、回放、存档和更正式的对局记录 UI。
4. 设施入场效果、企业图标效果、建设牌图标类效果仍未完成结构化结算。
5. 城市样式仍缺设施旋转/翻转匹配、旋转占用视觉化、特殊行动联动和更完整结算明细。
6. 自动跑局仍未覆盖特殊行动和更多事件/路费组合。
7. 联网仍缺完整断线重连、状态版本校验、确认命令缺口补发和更完整的拒绝原因回传。
8. 存档、回放、日志详情和正式设置项仍待扩展。

## 删除边界

本次未删除 `docs/建设牌输出` 或 `docs/城市样式牌输出`。

原因：

- `docs/建设牌输出` 仍被运行时代码直接读取，删除会破坏正式建设牌数据和右侧建设卡图加载。
- `docs/城市样式牌输出` 虽已被运行时资源和硬编码定义吸收，但包含 8 个文件和子目录；根据项目约束，不能批量删除文件或目录。如需清理，应由用户手动删除，或逐个明确文件单独确认后处理。
