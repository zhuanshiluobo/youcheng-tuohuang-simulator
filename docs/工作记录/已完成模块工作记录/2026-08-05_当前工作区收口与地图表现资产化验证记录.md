# 当前工作区收口与地图表现资产化验证记录

## 1. 本次范围

本次以 `codex/special-actions-and-interaction-fixes` 当前工作区为准，收口地图表现坐标资产化、Steam 双人验证入口、旧阶段/房间抽象清理及其配套文档，并重新建立完整 EditMode、Windows Development Build 和构建产物自动跑局证据。

本次没有提交或推送 Git，也没有清理 `.codex/`、`tmp/`、`tmp_*`、`tools/__pycache__/` 等既有未跟踪内容。

## 2. 实现与静态审计结论

- 四人地图地点、资源标记、地块影响力槽和航道影响力槽坐标已迁入 `Assets/Resources/MapLayouts/map-four-players.asset`。
- `MapDisplayLayout` 负责地图 Sprite、地点锚点、资源/槽位相对偏移和航道槽位锚点；`MapCoordinateSpace` 统一处理 Sprite 局部坐标与世界坐标换算。
- `MapViewPresenter` 统一从当前地图布局资产构建热点、资源标记与影响力槽，不再依赖四人地图硬编码坐标类。
- 构建前检查会验证地图 id、Sprite 绑定、地点/航道覆盖、槽位数量和归一化坐标范围。
- 已移除未被当前主流程引用的 `PhaseFlow`、`IRoomService`、`LocalRoomService`、旧四人地图坐标工厂和 `MapRouteDisplayController`；GUID 审计确认生产场景和资产中没有残留引用。
- 阶段合法性继续以 `CommandPhasePolicy` 和实际 Handler / `RoundAdvanceService` 为事实源；新增 `CommandPhasePolicyTests` 覆盖允许与拒绝路径。
- Steam 双人验证房仍要求 `playerCount=2` 与 `sessionKind=steam_two_player_validation` 同时成立，普通创建房间入口仍固定创建四人房。
- `git diff --check` 在测试前通过，没有空白符错误。

## 3. 自动验证

### 定向 EditMode

- 地图布局与坐标换算：`Logs/EditModeTests/editmode-20260805_105643.xml`，`11/11 Passed`。
- Steam 核心：`Logs/EditModeTests/editmode-20260805_105739.xml`，`42/42 Passed`。

### 完整 EditMode

- 结果文件：`Logs/EditModeTests/editmode-20260805_105823.xml`
- 结果：`817/817 Passed`
- 失败、跳过和不确定项：均为 `0`
- 最终消除 `LocalMirrorRoomService.LobbyJoinRequested` 未使用事件警告后，又通过当前 Editor 的 Unity MCP 测试任务 `fc3fd216b951496dbbf93c72e39e1dff` 重跑：`817/817 Passed`，失败与跳过均为 `0`。

当前测试总数与 2026-07-28 的 `818/818` 不同，是因为本轮删除了已失效测试并增加/调整了当前策略与地图测试；本次无过滤的 `817/817` 是当前工作区基线。

### Windows Development Build

- Unity MCP 任务：`build-f53708e264`
- 结果：`succeeded`
- 耗时：`5.0454935s`
- 目标：`StandaloneWindows64`
- 产物：`Builds/LocalhostDevelopment/tuohuang.exe`
- 玩法程序集：`Builds/LocalhostDevelopment/tuohuang_Data/Managed/Assembly-CSharp.dll`，生成时间为 2026-08-05 11:19:36
- 构建报告：`0` 错误、`0` 警告，总大小约 `302.5 MB`

当前 Editor 已通过 Unity MCP 连接时，构建使用 `manage_build(action="build", target="windows64", development="true", output_path="Builds/LocalhostDevelopment/tuohuang.exe")`，没有再启动第二个 Unity Editor。构建前的批处理启动尝试均未进入日志阶段，不作为项目编译失败证据。

### 构建产物自动跑局

- 日志：`Logs/LocalhostBuild/localhost-development-autoplay-20260805_112125.log`
- Player 退出码：`0`
- `Success: True`
- `Round: 8/8`
- `Phase: FinalScoring`
- `ExploreLocationSuccesses: 1`
- `MoveCitySuccesses: 1`
- `DispatchInfluenceSuccesses: 1`
- `BuildFacilitySuccesses: 9`
- `CharacterCardUseSuccesses: 1`
- `DeclareCityStyleSuccesses: 2`
- `SpecialActionSuccesses: 2`
- `DeployInfluenceSuccesses: 12`
- `PaidRouteCollectionSuccesses: 1`
- `OpponentRouteRecipientCollectionSuccesses: 1`
- `CharacterCardExecutions` 包含 `discardCount=1`
- `FinalScoringResolved: True`

## 4. 当前边界

自动化已经证明当前代码可编译、可构建并可从构建产物推进到最终计分，但以下内容仍需人工验收：

- 地图锚点、资源标记、地块/航道槽位和点击区域在实际画面中的对齐。
- 可调整窗口、非基准分辨率和后续地图 Transform 变化下的版面与点击换算。
- 行动按钮按压、地图边框脉冲、确认圆环、特殊行动拖拽和待选恢复手感。
- Editor Host + Development Build Clients 多进程。
- 两个 Steam 账号的双人验证房，以及标准多人房 P2P/Relay。

## 5. 下一步

先完成地图与交互人工 UI 冒烟，再进行多进程和真实 Steam 验收。上述验收收口后，地图技术线可继续实现只修改地图 Transform 或相机的平移/缩放 MVP；产品功能线再进入三人地图真实数据。
