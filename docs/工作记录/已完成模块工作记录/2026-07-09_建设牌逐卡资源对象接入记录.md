# 设施卡逐卡资源对象接入记录

## 完成内容

- 将 `docs/建设牌输出` 中 41 张普通设施卡图片和 4 张备用设施卡图片逐张复制到 `Assets/YC/Presentation/Resources/CardImages/Facilities/`。
- 资源统一按卡牌 ID 命名：`building_001.jpg` 至 `building_041.jpg`、`reserve_001.jpg` 至 `reserve_004.jpg`。
- Unity 已为 45 张图片生成独立 `.meta`，每张牌均成为可单独加载的资源对象。
- `FacilityCardDatabase` 的图片路径改为 `Assets/YC/Presentation/Resources/CardImages/Facilities/<卡牌ID>.jpg`，与城市样式牌使用同一套 `Resources` 加载方式。
- 更新设施卡数据库测试，覆盖 41 张普通设施卡图片和 4 张备用设施卡图片的逐卡资源路径。

## 清单迁移

- 设施卡运行时清单已迁入 `Assets/StreamingAssets/YC/Data/building_cards_manifest.json`。
- `FacilityCardDatabase` 已移除全部 `docs/建设牌输出` 候选路径，编辑器与桌面构建均从 `StreamingAssets` 读取清单。
- 运行时牌面图片已不再依赖 `docs/建设牌输出/建筑卡片` 和 `docs/建设牌输出/备用卡片`。
- `docs/建设牌输出` 与 `docs/城市样式牌输出` 均已不再是运行时依赖，可按项目删除规则手动清理。

## 验证

- 普通设施卡图片及 `.meta`：41/41。
- 备用牌图片及 `.meta`：4/4。
- 全量 EditMode：261/261 通过。
- `git diff --check`：通过。

> 数量口径：4 张备用设施卡图片/定义对应核心指挥塔 1 种与延伸枢纽 3 种；规则书实体备用卡为 4 张核心指挥塔和 3 张延伸枢纽，共 7 张。
