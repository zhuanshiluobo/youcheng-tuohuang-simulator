# 2026-07-07 建设牌运行时接入记录

## 背景

`docs/建设牌输出/building_cards_manifest.json` 已整理出正式建设牌数据，但运行时 `FacilityCardDatabase`、公开供应区和右侧建设卡区仍主要使用旧设施牌定义。本次工作把建设牌清单接入运行时代码，让建设供应区展示和发牌来源转向正式牌堆。

## 已完成

- `FacilityCardDatabase` 现在以 `docs/建设牌输出/building_cards_manifest.json` 为优先数据源：
  - 读取 41 张正式建设牌作为普通设施牌堆。
  - 读取 `核心指挥塔` 与 3 张 `延伸枢纽` 作为备用牌定义。
  - 保留硬编码兜底定义，避免文档文件缺失时数据库为空。
- `FacilityCardDefinition` 扩展字段：
  - `Color`
  - `UniqueGroupId`
  - `Description`
  - `EffectText`
  - `ManifestId`
  - `ReserveOnly`
  - `ImageRelativePath`
- `DefaultSupplyIds` 改为 41 张正式建设牌 id；备用牌不进入普通公开供应。
- 初始建设牌堆按 `eventDeckSeed` 确定性洗牌后翻出 6 张公开供应牌，右侧建设卡区不再固定显示旧的前几张设施牌。
- 建设唯一限制改为按 `UniqueGroupId` 判断，同名唯一牌即使来自不同 manifest id，也不能被同一玩家重复建造。
- 自动跑局的可建造设施筛选同步使用唯一组判断，避免自动选择已被同名唯一限制挡住的牌。
- 右侧建设卡区继续复用现有 `ImageRelativePath` 加载逻辑，路径指向 `docs/建设牌输出/建筑卡片` 与 `docs/建设牌输出/备用卡片`。

## 测试覆盖

- `BuildFacilityCommandHandlerTests` 新增/更新覆盖：
  - `FacilityCardDatabase_LoadsFormalBuildingCardsManifest`：确认 41 张正式建设牌均可查，备用牌不进默认供应，物流枢纽成本和图片路径来自正式数据。
  - `GameLaunchStateFactory_InitializesFacilitySupply`：确认初始公开供应区为洗牌后的 6 张正式建设牌，牌堆剩余数量正确。
  - `BuildFacility_WhenUniqueAlreadyBuilt_FailsWithoutMutating`：确认同名唯一牌不同 id 也会被拒绝。
  - `BuildFacility_WithGoldCost_SucceedsPaysGoldAndAppliesOnBuiltReward`：同步 `源石精炼厂` 建成奖励为 6 源石碎片。

## 验证结果

已通过的定向 EditMode：

```text
BuildFacilityCommandHandlerTests: 11 passed / 0 failed
PresentationSelectionControllerTests: 9 passed / 0 failed
FinalScoringServiceTests: 5 passed / 0 failed
```

静态检查：

```powershell
git diff --check
```

结果：无空白错误。

全量 EditMode 未完成：运行时多次被其他 Unity batchmode / smoke build 占用当前项目锁，本次未强行结束外部进程。

## 剩余边界

- 建设牌图标类效果仍以自然语言和 `EffectType` 粗分类记录，尚未接入统一结构化效果系统。
- `物流枢纽` 触发延伸枢纽建设、企业等级图标、免费航道影响力等特殊效果仍待后续规则实现。
- 当日记录时 `docs/城市样式牌输出/city_style_cards_manifest.json` 尚未接入 `CityStyleDatabase`；后续 2026-07-08 已接入 6 张城市样式的运行时定义、基础匹配、宣告次数和图片路径。
