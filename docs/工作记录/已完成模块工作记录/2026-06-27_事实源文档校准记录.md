# 2026-06-27 事实源文档校准记录

## 背景

`README.md`、当前工作总结与项目总览中仍保留了若干早于当时代码的描述，主要集中在三类问题：

- 仍把建设、采集、收尾推进或最终计分写成“未实现”。
- 未明确指出哪些按钮已经接通、哪些按钮仍是占位。
- 将本地联机自动跑局描述得比当前代码实际覆盖范围更宽。

本次以当前代码为准，重新校准事实源文档。

## 核对依据

- `Assets/YC/Application/Gameplay/BuildFacilityCommandHandler.cs`
- `Assets/YC/Domain/Facilities/BuildFacilityService.cs`
- `Assets/YC/Presentation/BuildFacilitySelectionController.cs`
- `Assets/YC/Domain/Scoring/FinalScoringService.cs`
- `Assets/YC/Application/Sessions/LocalhostAutoplayRunner.cs`
- `Assets/YC/Tests/EditMode/LocalhostAutoplayRunnerTests.cs`
- `Assets/YC/Presentation/MobileCityInteractionController.cs`
- `Assets/YC/Presentation/GameSettingsMenuController.cs`

## 核对结论

### 建设

- 建设已形成规则闭环：支持设施供应校验、唯一设施限制、城市面板槽位、资源/金券支付、建成奖励、即时得分和设施放置记录。
- 建设已形成表现层闭环：行动面板中的 `建设` 按钮会打开设施与支付方式弹窗，并提交 `BuildFacility` 命令。
- 因此，事实源文档不应再把“建设”列为未实现。

### 最终计分

- `FinalScoringService` 仅允许在 `FinalScoring` 阶段结算。
- 当前实际结算项目为：
  - `BaseScore`
  - `RegionScore`
  - `ResourceScore`
  - 平局时先比剩余金券，再比剩余至纯源石
- `FacilityScore`、`CityStyleScore` 字段已预留，但当前实现仍写入 `0`。
- 因此，事实源文档应改为“最终计分基础闭环已落地，但设施分与城市样式分尚未接通”。

### 占位按钮

- 已接通：`建设`、`规则书`、`返回主菜单`。
- 仍为占位提示：`使用角色牌`、`宣告样式`、`特殊行动`。
- 仍为空白占位：设置菜单主体区域。

### 自动跑局

- 当前自动跑局覆盖：
  - 固定四席位
  - 固定四人初始点
  - 入场默认选项解析
  - 行动阶段优先建设、其次部署影响力
  - 为各玩家提交采集命令
  - 收尾推进到 `FinalScoring`
  - 快照输出设施、影响力、采集和最终计分摘要
- 当前自动跑局不覆盖：
  - 探索
  - 城市移动
  - 调度
  - 特殊行动
  - 付费扩路采集
- 同时，采集目标当前默认只尝试玩家所在城市地块上的资源点，不能再写成“覆盖真实付费扩路采集路径”。

## 文档改动

- 更新 `README.md`：修正“已落地/仍未完成”口径，明确建设、采集、收尾推进和最终计分基础闭环已落地，并补充占位按钮说明。
- 更新当前工作总结（现位于 `docs/工作记录/当前工作总结.md`）：改为当时状态摘要，明确最终计分范围、占位按钮和自动跑局真实覆盖边界。
- 更新项目总览（现位于 `docs/概览/项目总览.md`）：作为事实源重写当时的闭环状态、限制和下一步，避免继续把旧状态当成现状。
- 更新 `docs/工作记录/README.md`：补入本条记录索引。

## 验证

- 本次仅修改文档，未改动运行时代码与测试代码。
- 本次未重新运行 Unity EditMode 测试；文档结论直接来自当前仓库代码与现有测试文件。
