# 2026-06-14 航道影响力坐标配置记录

## 工作目标

根据 `docs/模块文档/航道位置配置与显示模块文档.md` 补充地图表现层中的航道影响力槽位坐标配置。由于地图路线已经由美术图确定，本轮不绘制航道线段，只保留航道影响力坐标，方便后续手动微调和接入槽位显示。

## 已完成

- 新增 `Assets/YC/Presentation/Maps` 目录，作为地图表现配置目录。
- 新增 `MapRouteDisplayDefinition`：
  - `MapId`：对应规则地图 id。
  - `RouteId`：对应规则航道 id。
  - `InfluenceSlotPositions`：该航道影响力槽位在地图图片上的归一化坐标。
- 新增 `FourPlayerRouteDisplayDefinitions`：
  - 覆盖四人地图 22 条航道。
  - 每条航道只保留 `RouteId` 和 `Slots(...)`。
  - 坐标使用与资源点一致的归一化坐标系：左上角 `(0, 0)`，右下角 `(1, 1)`。
- 删除本轮早期草案中与路线绘制相关的内容：
  - `Points(...)`
  - `NormalizedPoints`
  - `LineRenderer`
  - 航道颜色、宽度、状态枚举
  - `MapRouteDisplayController`
- 清理 `MobileCityInteractionController` 中临时接入的航道绘制初始化和刷新调用。
- 未修改规则层航道定义、邻接查询、寻路、路费、区控或影响力规则。

## 涉及文件

- `Assets/YC/Presentation/Maps/MapRouteDisplayDefinition.cs`
- `Assets/YC/Presentation/Maps/FourPlayerRouteDisplayDefinitions.cs`
- `Assets/YC/Presentation/Maps.meta`
- `Assets/YC/Presentation/Maps/MapRouteDisplayDefinition.cs.meta`
- `Assets/YC/Presentation/Maps/FourPlayerRouteDisplayDefinitions.cs.meta`
- `Assets/YC/Presentation/MobileCityInteractionController.cs`

## 验证情况

- 文本检查确认 `Assets/YC/Presentation/Maps/*.cs` 中不再包含：
  - `Points(`
  - `NormalizedPoints`
  - `LineRenderer`
  - `RouteDisplayState`
  - 线条颜色和宽度字段
- 文本检查确认 `MobileCityInteractionController` 中不再引用航道绘制控制器。
- 脚本校验四人地图 22 条航道配置齐全。
- 脚本校验每条航道 `Slots(...)` 数量与规则层 `InfluenceSlotCount` 一致。
- `git diff --check` 通过。
- 未运行 Unity Test Runner。

## 后续事项

- 后续需要实现统一的影响力槽位显示控制器时，应读取 `FourPlayerRouteDisplayDefinitions` 中的 `InfluenceSlotPositions`。
- 航道槽位坐标可直接在 `FourPlayerRouteDisplayDefinitions.cs` 中手动调整。
- 若三人地图规则航道稳定后，再新增对应地图 id 的航道影响力槽位配置。
- 表现配置不得参与规则判断；规则仍以 `StaticMapDefinitions`、`MapQueryService` 和影响力服务为准。
