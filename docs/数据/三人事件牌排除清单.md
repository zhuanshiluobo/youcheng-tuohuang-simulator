# 三人事件牌排除清单与 B 规则交接

日期：2026-09-08。状态：白名单实现及静态核对完成；未启动 Unity，未编译或运行 EditMode 测试，未生成 meta、未提交。不能据此宣称三人整局验收通过。

## 1. 原始牌面核对

事实源为 `游城拓荒/素材/绿色.jpg`、`红色.jpg`、`黄色.jpg` 三张原始整版牌面，已逐张查看标题角标。运行时 ID 另对照 `Assets/YC/Editor/Data/event_character_cards_manifest.json`；拆图文件编号与运行时编号不同，不能直接按拆图文件名过滤。

| 颜色 | 牌名 | 原整版位置 | 运行时 ID | 处理 |
| --- | --- | --- | --- | --- |
| 绿 | 废弃矿坑 | 第3行第2列 | event_green_06 | 标4，三人排除 |
| 红 | 深层矿床 | 第3行第1列 | event_red_05 | 标4，三人排除 |
| 黄 | 情报交换 | 第3行第1列 | event_yellow_05 | 标4，三人排除 |
| 黄 | 敌对生态圈 | 第4行第2列 | event_yellow_08 | 标4，三人排除 |

其余当前有效牌标题没有标4：

- 绿：中立采石场、清理虫巢、外露矿床、富饶岩层、矿业聚落。
- 红：险中净土、裂谷矿脉、采集平台残骸、高污染环境、地质瑰宝。
- 黄：大型源岩场、洞穴遗迹、荒地人村落、锈锤领地、风险任务、异铁开采权、富异铁区、遗弃矿场（现有拆图文件称“遭弃矿场”）。
- 原整版红色另有采集平台残骸企业版，黄色另有洞穴遗迹企业版及占位符，均不是标4牌，且未列入现有22张有效运行时目录；本次不扩充目录。

三人保留：绿5、黄8、红5，共18。四人保留：绿6、黄10、红6，共22。四张共享定义及资源资产均不删除。

## 2. 最小 API 与调用交接

```csharp
public void InitializeDecks(
    DeckRuntimeState decks,
    IReadOnlyList<string> greenCardIds,
    IReadOnlyList<string> yellowCardIds,
    IReadOnlyList<string> redCardIds,
    int playerCount = 4)
```

- `EventCardDatabase.IsFourPlayerOnly(string cardId)` 用固定四项运行时 ID 判断，不依赖显示名称、Unity 目录初始化或新序列化字段。
- **先从输入源列表复制并过滤，再调用现有 InitializePool 洗牌**；不修改调用者传入列表。早期“洗牌后过滤”已按用户后续明确要求修正。
- 只有 `playerCount == 3` 执行过滤。默认4及单人四人开发探针保留旧完整牌池和原随机序列。不新增人数验证，以免阻断既有开发路径。
- 重建仍使用本 API；相同人数、相同原始列表与相同种子新建服务可重现。不要在已消耗随机数的服务实例上期待重建到初始顺序。
- 当前全部调用点（不含定义）：
  - `Assets/YC/Presentation/GameSessionBootstrapper.cs`：生产启动；最终复查已由主 agent 传入 `playerCount`：有启动上下文时取 `launchContext.Players.Count`，无上下文开发探针回退4，保留完整四人牌池。
  - `Assets/YC/Application/Sessions/LocalhostAutoplayRunner.cs`：跑局；复查时已由其他负责人传入 `playerCount: seats.Count`。
  - `Assets/YC/Tests/EditMode/NetworkCommandDispatcherTests.cs`：两处旧默认人数调用，继续表示完整四人牌池的确定性测试，不代表三人联机验收。
  - `Assets/YC/Tests/EditMode/ThreePlayerRulesTests.cs`：本次新增，默认参数调用及传入真实测试人数的帮助函数；测试中另有手工过滤源列表构造预期顺序的调用。
- Host、Client 的启动、重放及恢复须由主 agent 确保真实席位人数和种子一致。直接恢复已有牌堆状态不会自动调用此过滤；不能把旧四人牌堆快照当作三人牌堆继续使用。本次不改网络/启动文件，也没有验证网络恢复。

## 3. 资金、红区、七区块静态结论

- `Assets/YC/Application/Setup/SetupCommandHandler.cs` 已在全员入场完成后按 TurnOrderService 顺序追加基础金券，三人10/12/18，四人10/12/14/18；不覆盖已有事件金券。本次未修改。
- `Assets/YC/Domain/Rules/RedZoneAccessRule.cs` 已按三人人数第5回合开放；若错误传入四人地图，人数会被地图范围夹到4，导致第4回合开放，因此正式图/人数一致性是集成前提。
- `Assets/YC/Domain/Exploration/ExplorationService.cs` 与 `Assets/YC/Domain/Movement/CityMovementService.cs` 已调用该规则；本次未改，也未实跑红区相邻航道操作。
- 原三人地图顶部明确第5回合才可探索红区或将城市移入红区。顶部 FINAL 给出 A/B/C/D/R 各3分、E/F 各4分。
- `Assets/YC/Domain/Scoring/RegionControlService.cs` 遍历传入地图 Regions，不硬编码8块；并列最高无控制者。`InfluenceQueryService.cs` 按城市额外2影响力计入；`FinalScoringService.cs` 使用每区 ScoreValue 结算。本次全部只读。真实7块及其成员由 A 录入，不以规则夹具替代生产图验收。

## 4. 独立原图复核，供 A 交叉确认

原图：`游城拓荒/地图/三人.jpg`（5000×5000）。先看整图，再从原分辨率截取18个事件图标和 B-01 奖励局部核对；局部仅为阅读证据，不改原图、不生成 Unity 资产。

- 绿：A-01、A-02、B-01、B-02、C-01。
- 黄：A-03、B-03、C-02、C-03、D-01、D-02、D-03、E-01。
- 红：E-02、E-03、F-01、F-02、F-03。
- B-01 标注“入场时获得”：2源岩图标、2异铁图标、2源石图标；对应 `Originium=2, Iron=2, OriginiumShard=2`。没有金券或分数图标。此为独立地图入场奖励，不应替代基础资金或事件选项奖励。录入与触发代码仍交 A/主 agent，不在本次擅自实现。
- 阅读截图在本地可视化目录：`C:/Users/hby/.codex/visualizations/2026/09/08/01a07f48-fab3-7c83-84e2-850b85c630b6/B01-source-crop.png`、`ThreePlayer-original-color-crops.png`。不属于仓库交付资产。
- 已在当前对话报告复核结论；未识别到可直接投递的 A 独立任务 ID，未声称已获得 A 的回签，请主 agent 转交并交叉确认。

## 5. 验证与未完成风险

新增 `Assets/YC/Tests/EditMode/ThreePlayerRulesTests.cs`：覆盖精确排除清单、源列表先过滤的顺序、共享列表不变、同种子重建、四人默认与单人探针完整牌池、三四人基础资金按顺序叠加、三人第4/5回合红区边界、四人第4回合不变、七区块数据驱动计分、城市影响力和并列控制。

资金测试通过反射隔离现有基础发放方法，不证明完整入场事件流程；七区块测试采用独立规则夹具，不证明 A 的正式拓扑。测试待主 agent 在允许操作 Unity 时运行，本次无运行通过结论。改动文件的 `git diff --check` 已通过。

白名单外集成风险准确路径（均未修改）：

1. `Assets/YC/Application/Setup/EntranceEventCardScenario.cs`。
2. `Assets/YC/Domain/Exploration/ExploreEventCardScenario.cs`。
3. `Assets/YC/Domain/Movement/MoveCityEventCardScenario.cs`。

复查时三者仍使用 `StaticMapDefinitions.GetEventColor(request.TargetId)`，须由地图查询负责人确认迁移到当前地图的颜色查询。本次未获这些文件写权限。

白名单内但尚需集成的入场门禁：`Assets/YC/Application/Setup/SetupCommandHandler.cs` 当前 IsInitialLocationAllowed 仅限制四人地图，正式三人绿色入场地点门禁应结合 A 冻结的地图查询 API 接入；基础资金正确不代表此门禁已完成。本次未擅自复制地图数据表。B-01 奖励触发也仍待 A/主 agent 接入。

本次实际修改：EventCardDatabase.cs、EventDeckService.cs；新增：ThreePlayerRulesTests.cs、本清单。EventCardDefinition.cs、SetupCommandHandler.cs 保持原样，避免无必要新字段与重复规则。
## 后续 Setup 授权更新（2026-09-08）

以下进展替代上文“Setup 未改/门禁和奖励待接入”的历史状态：SetupCommandHandler 已修改，两处 EntranceEventCardScenario 构造均注入 resourceTokenService、mapQueryService。三人绿色门禁读取当前地图地点 EventColor；四人仍使用原入场点门禁。

首次合法初始城市落位后，仅此处向玩家追加 location.InitialEntranceReward；不在事件选项解析或 GrantInitialGoldVouchers 内发放。已有城市/待选/阶段门禁阻止重复入场。A 已经用户确认提供 ResourceSet 类型 InitialEntranceReward（默认空），三人 B-01 三种资源各2，四人空。不要在 EntranceEventCardScenario 再次发放地图奖励。先前临时 B-01 硬编码分支已撤除。

新增测试覆盖非绿色拒绝、地图资源奖励一次性、共享奖励未消耗及不追加金券（已有资源标记隔离事件流程）；尚未编译或执行。首轮编译需 A 的 EventColor、InitialEntranceReward 字段及双参数场景构造全部落盘，仍由主 agent 启动。